- name: Keybindings | Map Caps lock to Esc
  shell: |
    setxkbmap -option caps:escape
  changed_when: false

- name: Keybindings | Map Caps lock to Esc
  shell: |
    xmodmap -e 'keysym Print = Super_L'
  changed_when: false
